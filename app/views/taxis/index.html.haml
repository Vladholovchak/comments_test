%h1 Taxis
- @taxis.each do |taxi|
  %p
    = taxi.car
  %h4 Comments
  = form_for [taxi, Comment.new] do |f|
    = f.text_area :body, placeholder: "Add a comment"
    %br/
    = f.submit "Add Comment"
  %ul
    = render(partial: 'comments/comment', collection: taxi.comments)
